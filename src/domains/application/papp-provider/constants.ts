// CAUTION (host-papp): stable across launches — it seeds the storage prefix backing
// every persisted SSO blob. Changing it orphans every existing pairing.
export const HOST_PAPP_APP_ID = 'Polkadot Desktop';
