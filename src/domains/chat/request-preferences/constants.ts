// Persisted user preference: hide incoming message-request content by default.
// Default `true` — request messages are hidden until the user reveals them
// (per-request "View message"), matching the "Message hidden based on your
// default settings" design.
export const HIDE_REQUESTS_BY_DEFAULT_STORAGE_KEY = 'pb:chat-hide-requests-by-default';
export const HIDE_REQUESTS_BY_DEFAULT = true;
